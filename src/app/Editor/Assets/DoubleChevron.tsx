import { HiOutlineChevronDoubleRight } from "react-icons/hi";

export default function DoubleChevron({
  size,
  color,
}: {
  size: number;
  color: string;
}) {
  return <HiOutlineChevronDoubleRight size={size} color={color} />;
}
