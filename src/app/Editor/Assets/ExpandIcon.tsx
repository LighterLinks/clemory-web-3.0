import { GoSidebarExpand } from "react-icons/go";

export default function ExpandIcon({ size, color }: { size: number; color: string }) {
    return (
        <GoSidebarExpand size={size} color={color} />
    );
}