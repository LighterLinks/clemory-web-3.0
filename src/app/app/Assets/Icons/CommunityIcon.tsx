import { RiCommunityLine } from "react-icons/ri";

export default function CommunityIcon({
  size,
  color,
}: {
  size: number;
  color: string;
}) {
  return <RiCommunityLine size={size} color={color} />;
}
