import { IoEllipsisHorizontalCircleSharp } from "react-icons/io5";

export default function EllipsisIcon({
  size,
  color,
}: {
  size: number;
  color: string;
}) {
  return <IoEllipsisHorizontalCircleSharp size={size} color={color} />;
}
