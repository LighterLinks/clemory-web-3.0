import { IoChevronBackOutline } from "react-icons/io5";


export default function LeftExpandIcon({
  size,
  color,
}: {
  size: number;
  color: string;
}) {
  return <IoChevronBackOutline size={size} color={color} />;
}