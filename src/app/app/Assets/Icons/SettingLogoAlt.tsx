import { LuSettings2 } from "react-icons/lu";

export default function SettingLogoAlt({
  size,
  color,
}: {
  size: number;
  color: string;
}) {
  return <LuSettings2 size={size} color={color} />;
}