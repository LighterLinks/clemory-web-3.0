import { IoGlobeOutline } from "react-icons/io5";

export default function WebIcon({
  size,
  color,
}: {
  size: number;
  color: string;
}) {
  return <IoGlobeOutline size={size} color={color} />;
}
