import { IoCopyOutline } from "react-icons/io5";

export default function CopyIcon({
  size,
  color,
}: {
  size: number;
  color: string;
}) {
  return <IoCopyOutline size={size} color={color} />;
}
