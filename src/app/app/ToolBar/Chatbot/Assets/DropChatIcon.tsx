import { PiChatTeardropText } from "react-icons/pi";

export default function DropChatIcon({ size, color }: { size: number, color: string }) {
    return (
        <PiChatTeardropText size={size} color={color} />
    )
}