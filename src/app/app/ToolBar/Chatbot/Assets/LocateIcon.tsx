import { IoLocateOutline } from "react-icons/io5";

export default function LocateIcon({
  size,
  color,
}: {
  size: number;
  color: string;
}) {
  return <IoLocateOutline size={size} color={color} />;
}
