import { PiPlusSquare } from "react-icons/pi";

export default function PlusSquareIcon({
  size,
  color,
}: {
  size: number;
  color: string;
}) {
  return <PiPlusSquare size={size} color={color} />;
}
