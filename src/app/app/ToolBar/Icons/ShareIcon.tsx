import { IoShareSocialOutline } from "react-icons/io5";

export default function ShareIcon({
  size,
  color,
}: {
  size: number;
  color: string;
}) {
  return <IoShareSocialOutline size={size} color={color} />;
}
