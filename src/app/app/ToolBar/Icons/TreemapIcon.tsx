import { TbChartTreemap } from "react-icons/tb";

export default function TreemapIcon({
  size,
  color,
}: {
  size: number;
  color: string;
}) {
  return <TbChartTreemap size={size} color={color} />;
}
