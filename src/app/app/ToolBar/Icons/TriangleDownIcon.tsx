import { GoTriangleDown } from "react-icons/go";

export default function TriangleDownIcon({
  size,
  color,
}: {
  size: number;
  color: string;
}) {
  return <GoTriangleDown size={size} color={color} />;
}
