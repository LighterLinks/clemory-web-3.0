import { GoTriangleUp } from "react-icons/go";

export default function TriangleUpIcon({
  size,
  color,
}: {
  size: number;
  color: string;
}) {
  return <GoTriangleUp size={size} color={color} />;
}
